## Instructions

- Use Vite build for production bundles.
- Set correct base path for deployment.
- Enable caching for static assets.

### Notes

- Use `vite build` and serve `dist/`.

Reference: https://vuejs.org/guide/best-practices/production-deployment.html
